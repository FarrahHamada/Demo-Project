<!doctype html>
<html>
<head>
    <title>Neighbor Profile</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="style.css">
</head>
<body id="profile">
<div class="container">

    <div class="page-header text-center">
        <h1><span class="fa fa-users"></span> Profile Page</h1>
        <a href="/logout" class="btn btn-default btn-sm">Logout</a>
    </div>

    <div class="row">

        <!-- LOCAL INFORMATION -->
        <!-- <div class="col-sm-6">
            <div class="well">
                <h3><span class="fa fa-user"></span> Local</h3>

                <% if (user.local.email) { %>
                    <p>
                        <strong>id</strong>: <%= user._id %><br>
                        <strong>email</strong>: <%= user.local.email %><br>
                        <strong>password</strong>: <%= user.local.password %>
                    </p>

                    <a href="/unlink/local" class="btn btn-default">Unlink</a>
                <% } else { %>
                    <a href="/connect/local" class="btn btn-default">Connect Local</a>
                <% } %>

            </div>
        </div> -->
        <div class="col-sm-6">
          <h3>Contact </h3>
          <!-- <ul class="messages">
          <% for(var i=0; i<messages.length; i++) {%>
            <li class="message">
              <span><%= messages[i].name %></span>
              <span><%= messages[i].msg %></span>
              <span><%= messages[i].thumbUp %></span>
              <span><i class="fa fa-thumbs-up" aria-hidden="true"></i></span>
              <span><i class="fa fa-thumbs-down" aria-hidden="true"></i></span>
              <span><i class="fa fa-trash" aria-hidden="true"></i></span>
            </li>
          <% } %>
          </ul> -->

          <p> NOTE: In order to add a post you must upload a photo that relates to your task.</p>
        <% if (user.local.email) { %>


          <form action="/post" enctype="multipart/form-data" method="POST">
            <label for="">Title</label>
            <input type="text" name="title" value="">
            <label for="">Details</label>
            <textarea name="details" rows="8" cols="80"></textarea>
            <input class="formFlexResize" type="file" name="file-to-upload"></input>

            <label for="">Email</label>

            <input type="text" name="name" value="<%= user.local.name %>">
            <input type="text" value="<%= user.local.email %>" name="email">
            <button type="submit">Submit</button>
          </form>
        </div>
        <% } %>
        <img src="<%= user.local.profileImg %>" alt="">

        <form action="/profileImg" enctype="multipart/form-data" method="POST">

          <input class="formFlexResize" type="file" name="file-to-upload"></input>


          <button type="submit">Submit</button>
        </form>
</div>

<script src="main.js"></script>

</body>
</html>
